name: hue_web
description: Philips Hue Web Controller
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^1.2.1
  provider: ^6.1.2

flutter:
  uses-material-design: true